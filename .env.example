# Origami environment variables.
# Copy to .env.local and fill in the keys you need.

# Required only if a bot flow uses an AI-response node.
OPENAI_API_KEY=

# Local SQLite database (relative paths resolved from project root).
DATABASE_URL=file:./origami.db
